- nxos_evpn_vni:
    host: '{{ inventory_hostname }}'
    password: '{{ pwd }}'
    route_distinguisher: '60:10'
    route_target_both: default
    route_target_export: auto
    route_target_import:
    - '5000:10'
    - 4100:100
    username: '{{ un }}'
    vni: 6000
