- aci_tenant_span_src_group:
    admin_state: '{{ admin_state }}'
    description: '{{ description }}'
    dst_group: '{{ dst_group }}'
    host: apic
    password: SomeSecretPassword
    src_group: '{{ src_group }}'
    tenant: production
    username: admin
