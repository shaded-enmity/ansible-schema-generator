- nxos_vtp_password:
    host: '{{ inventory_hostname }}'
    password: '{{ pwd }}'
    state: present
    username: '{{ un }}'
- nxos_vtp_password:
    host: '{{ inventory_hostname }}'
    password: '{{ pwd }}'
    state: absent
    username: '{{ un }}'
