- bigip_monitor_udp:
    ip: 10.10.10.10
    name: my_udp_monitor
    password: secret
    server: lb.mydomain.com
    state: present
    user: admin
  delegate_to: localhost
  name: Create UDP Monitor
- bigip_monitor_udp:
    name: my_udp_monitor
    password: secret
    server: lb.mydomain.com
    state: absent
    user: admin
  delegate_to: localhost
  name: Remove UDP Monitor
